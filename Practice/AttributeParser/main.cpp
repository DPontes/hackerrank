#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;

int main() {
    int n_lines, n_queries;
    vector<string> v;
    string line;
    cin >> n_lines;
    cin >> n_queries;

    while(getline(cin, line)) {
        v.push_back(line);
    }

    return 0;
}

