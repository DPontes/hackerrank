# Overloading Ostream Operator

[Link to problem description](https://www.hackerrank.com/challenges/overloading-ostream-operator/problem)

To compile and run the program with the input file:

```
> c++ main.cpp -o run.out -std=c++14
> ./run.out < input.txt
```

## Expected output for input.txt

```
first_name=john,last_name=doe registered
```
